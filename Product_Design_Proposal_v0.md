# OKX Agentic Wallet PM · 产品设计方案

> **TL;DR（30 秒读完）**
> - **核心判断**：Agent 钱包当前有三个约束——**可用性**（看不懂代码 / 合约 / 授权，一面反馈里最直接的痛点）**× 信任**（链上不可逆，一笔错全没）**× 商业模式**（收不到费）。初版先解前两个：可信执行通道 + 可编程授权 + 标准接口。**这是我按当前阶段的排序假设，不是定论**——上线后用漏斗数据验证（§1.2）。
> - **商业逻辑**：平台卡住 Agent 经济中供需交互的**四个收费站**（交易手续费 / 订阅 + 利润分成 / 结算通道费 / **broker 返佣**），把钱包做成 Agent 的执行层与价值层。对标 Coinbase 2026 Q2：非交易收入已占净收入 **48%**，而交易收入同比 **−21%**——**收入结构必须换腿**。
> - **我的第一件事**：先在一条链上跑通一个真实高频闭环（内部 X Layer 打样 → Solana 复制），拿到数据和用户后再扩展——不铺摊子。
> - **一句话定位**：跟单已经证明"为结果付费"成立（OKX 现货 / 合约跟单分润 8%–13%、策略带单最高 30%），但它的边界是**信号源只能是"人"、信号与执行锁死在同一个 CEX 账户内**。Agentic Wallet 做的是把这套**已被验证的结算范式外溢**：信号源扩到任意 Agent / MCP 服务，执行场所扩到链上 + CEX，激励结构不变——信号可以黑盒，每一次执行都透明。

## 第一部分：OKX 现状 & What should OKX do first

### 1.1 OKX 现状
1. OKX 2026 两件新品：**Agentic Wallet**（链上，为 Agent 设计，TEE + 模拟 + 风险分级）+ **Agent Trade Kit**（CEX，MCP 自然语言下单）。
2. OKX 正在把钱包的能力开放给 AI Agent，让 Agent 自己持有、自主交易，同时用授权限额、先模拟、风险分级、TEE 私钥隔离给足护栏。

### 1.2 从第一性原理推出"Agentic Wallet 做什么"

**三个约束同时成立，我按当前阶段排序（这是假设，不是定论）**：
1. **可用性**——用户看不懂代码 / 合约 / 授权。这是一面反馈里最直接的痛点，排第一。
2. **信任**——链上不可逆，把真金白银的私钥 / 授权交给一个不可控的 Agent，一笔错全没。
3. **商业模式**——现有收入 = "用户主动高频交易才收得到钱"，用户越躺平这池子越萎缩。

> **我不会把这个排序当结论，上线后用漏斗数据验证它**：
> 新增用户卡在"首笔授权"流失 → 说明 ① 是瓶颈；授权完成但资金规模上不去 → 说明 ② 是瓶颈；用户和量都起来了但收入不起 → 说明 ③。
> **排序是假设，数据才是结论。**（验证方法见 §3.2）

**所以初版只做三件事**：**一个可信执行通道 + 一套可编程授权（限额 / 期限 / 白名单）+ 一组 Agent 可调用的标准接口**，外加先模拟、风险分级、TEE 兜底。先跑通一条链上的真实用例（如 Agent 自动支付 / 自动申购），拿到数据再扩链。

**可编程授权的第一道防线应该在「工具注册期」，不是执行期**——这是 Agent 安全和传统 API 权限的根本差异。Agent Trade Kit 已经这么做了，官方原文：

> *"API key permissions are read on startup. If a key lacks trading permission, order tools are **never registered** in the agent's toolbox."*

运行时拦截意味着 Agent 已经"想"过要下单了；**注册期根本不注册，意味着它不知道自己有这个能力**。配合密钥存本地配置、本地 HMAC-SHA256 签名、**从不传给 LLM**，这三件套一起构成"可编程授权"的现成实现——我要做的是把它从 CEX 侧平移到链上侧。

→ conclusion:
> 三个约束里，先解「敢不敢用」（可用性 × 信任），再解「收不收得到钱」——**顺序不能反，因为前两个决定了池子里有没有水**。
> 站在 AI × Web3 交汇点，把钱包做成 Agent 的执行和价值层。

---

## 第二部分：Agentic Wallet 产品设计

### 2.1 背景：为什么 OKX 现在必须做 Agent

**"OKX 收不到费"的现状：**
- 链上交易：gas 全给矿工 / 验证者、流动性费给 DEX / LP，OKX 能截留的部分极薄——
  - **DEX 聚合器是分层接口费**（Other↔Other **0%**、G1↔G1 **0.10%**、G1/G2↔Other **0.50%**，普遍 0.1%–0.3% per swap），但中文物料又宣称"平台不收取任何额外平台手续费"——**官方自身口径就不统一，散户侧的收费感知极弱，这本身就是问题**；
  - **DEX API 更关键**：官方开发者文档原文是 *"Currently the OKX DEX API charges no fees"*，同时明确 *"plans to take a certain percentage"*（集成方最多可向终端用户加收 3% 推荐费）。**白纸黑字写"现在不收、但计划要收"——这是"预留收费站"最硬的证据。**
- CEX：现货 maker / taker 仅 0.08% / 0.10%、合约 0.02% / 0.05%，散户手续费薄，大头在机构做市商。
- **结论：现有收入 = "用户主动高频交易才收得到钱"。** 用户越自动化越躺平，这池子越萎缩——这正是 OKX 押注 Agent 方向的动因：**换一条收钱的路：代理结算 + 订阅服务费。**

**官方已在落地的产品：**
- **Agentic Wallet**：官方口径是**密钥的生成、存储、签名全部在 TEE 内完成，LLM 和 Agent 都接触不到私钥 / 助记词**；支持邮箱 / Google / Apple 登录、恶意授权一键撤销、**最多 50 个子钱包分仓隔离**、基于 x402 的自动支付，已接入近 20 条链——"钱包给 Agent 用"已上线。（**50 个子钱包是 §2.8.2 归因方案的地基**）
- **Agent Trade Kit**：Agent 经 MCP 直接在 CEX 下现货 / 合约 / 期权单，带 Demo 模拟盘。
- **Onchain OS + Agent Payments Protocol（APP）**：Agent 支付标准（底层 EIP 获以太坊基金会支持），X Layer 零 gas、单笔可低至 1 美分，支持 escrow / 按调用 / HTTP 402。**口径统一一下：APP 是协议层标准，在 Onchain OS / Agentic Wallet 侧以 x402 落地**——也就是 §2.6 阶段 2 说的"x402 自动支付"和这里的 APP 是同一件事的两层说法。Onchain OS 当前规模：日 **12 亿次** API 调用、日交易量 **$3 亿**、**<100ms**、99.9% 可用性、聚合 **500+ DEX**。
- **OKX.AI marketplace**（2026-06 上线）：Agent Marketplace + Task Marketplace 双场；A2MCP（按次即时结算）与 A2A（托管担保 + 仲裁）两种模式；结算币种 USDT / USDG，用户 3 天验收期超时默认通过。**抽佣管道其实已经预埋**——仲裁者需质押 ≥100 OKB、每次 ≥5 名、少数服从多数，判对者瓜分**任务佣金的 5%**；ASP 发起申诉需另付任务报酬的 **5%** 作保证金。**所以缺的从来不是管道，是开闸时机与费率档位设计**——这正是 §2.5 要回答的。
- **参照系就在自家**：OKX 现货 / 合约跟单分润 **8%–13%**、策略带单最高 **30%**，按自然周结算、系统先预扣后退（本质就是 high-water mark 的工程实现）——**"为结果付费"这个模式在 OKX 已被验证三年**。所以我的命题不是"要不要做 profit-share"，而是"把这套已验证的结算范式，从'人 + 单一 CEX 账户'外溢到'任意 Agent + 链上/CEX'会卡在哪"——答案见 §2.8.2 归因。

早期生态合作方 CoinAnk 即"链上实时行情数据按次收费"——**"卖家经 MCP 卖信号、黑盒不透传、买家付费调用"就是 OKX.AI 的 A2MCP 形态。注意它是按次，不是按月**——这正好印证 §2.8.3：买方侧的"月度订阅"目前是缺的，需要新增订阅凭证层。

### 2.2 产品定位：Codex 的壳 + Siri 的门槛
- **"Codex 的壳 + Siri 的门槛"**——人话进去、链上结果出来、中间全帮用户做完。

### 2.3 产品骨架
- **要卖的结果** = 用户达成目标（买指定币 / 止损 / 定投 / 付账 / 资产增值），不是钱包，也不是代码。
- **核心瓶颈** = 认知门槛（看不懂代码 / 合约 / 授权）**×** 信任（链上不可逆，一笔错全没）。
- **解法 = "意图"与"链上执行"之间加三层**：

```
用户(人话) → ①意图层   → ②护栏层   → ③执行层 → 结果 + 可追踪记录
            解意思/拆步骤  模拟·限额·分级·TEE   Agent Trade Kit(CEX)
            面向小白不卡壳   信任问题唯一解       Onchain OS(链上)/APP(支付)
```

### 2.4 用户分层：怎么让不同水平用户都能用
| 用户层 | 真实诉求 | 产品形态 | 对应官方部件 |
|---|---|---|---|
| 完全小白 | "我想买点比特币" | 对话交易 + 一键钱包 | Agentic Wallet |
| 普通用户 | "跌破 9 万就买，8.5 万止损" | 意图命令（条件单） | Agent Trade Kit / DEX 聚合 |
| 进阶用户 | "帮我按这信号定投" | 订阅 marketplace 信号（按月，凭订阅凭证调用） | A2MCP + **订阅凭证层**（新增，§2.8.3） |
| 卖家 / 开发者 | "我的策略想卖订阅" | 黑盒上架，计费托管平台负责，逻辑不透传 | Onchain OS + APP |

### 2.5 商业闭环：四腿收钱（OKX 为什么赚得到钱）
1. **交易手续费**——Agent 替用户高频买，量上去了（现货 0.080%/0.100%、合约 0.020%/0.050%，薄但可被 Agent 的交易频次放大）。
2. **卖家收入抽成**——**订阅费（现金流底盘）+ 利润分成 profit-share（激励校准层）两层叠加**；分成的 P&L 来自专属子钱包账本（§2.8.2），不靠卖家自报。
3. **Agent 结算通道费**——escrow / 微支付通道（X Layer 零 gas、单笔最低 1 美分，微支付在经济账上才成立）。
4. **Broker 返佣**——Agent Trade Kit 官方明确：*"Every trade through your product earns you rebates by Broker fee tier — **up to 50%**."*

> **第 4 条在冷启动期最值钱**，因为它是四条腿里**唯一不依赖 marketplace 起量**的一条：任何接入 OKX 的第三方 Agent 产品，只要产生交易，OKX 就有分成。前三条都要等 marketplace 双边起来才有量，**而 broker 返佣从第一个接入方、第一笔交易就开始收——marketplace 还是空的时候，这条腿能先跑出现金流。**

**为什么这个方向是对的：看 Coinbase 已经跑出来的结果（2026 Q2）**

| Coinbase 2026 Q2 | 数字 | 含义 |
|---|---|---|
| **订阅与服务收入** | **$555M = 净收入的 48%** | Q4 2024 才 29%，Q2 2020 仅 $6M |
| 交易收入 | $599M，同比 **−21%** | 交易费在被竞争压薄 |
| 净亏损 | $359M | 但它**市场份额反而创新高到 10.3%** |

> **这组数字说明的不是"订阅比交易好赚"，而是：当交易费被竞争压薄时，非交易收入才是活下去的那条腿。**
> 我们要复制的不是它的订阅**产品**，是它的**收入结构转型**——这也正是 §2.1「OKX 收不到费」那个判断的对标答案。

**平台垄断了 Agent 经济里供需交互的三个必经环节，每个环节是独立收费站**（①–③ 对应平台内的三条流；④ broker 返佣是"平台外"的第四条——靠别人家的产品帮我们带量）：
- 执行引擎（撮合 / 路由 / 结算）堵住**资金流** → 按交易额抽成 = ① 交易手续费
- Marketplace（双边网络）堵住**信息 / 供需流** → 按卖家收入分成 take rate = ② 订阅 + 利润分成（profit-share，收的是**卖家**的钱；分成口径由子钱包账本定，见 §2.8.2）
- 托管 / 担保 / 仲裁（escrow）堵住**风险流** → 按担保 / 结算金额收费 = ③ 结算通道费
- 商业学本质：三种价值单元互相独立——①按"动作价值"计费、②按"卖家持续收入"计费、③按"风险承担量"计费，谁都不是谁的别名。
- 类比收口：交易手续费 = 银行通道费；订阅 + 利润分成 = App Store 的 30%；结算通道费 = escrow 担保费；broker 返佣 = 渠道分销返点（别人的货架，我们抽成）。

### 2.6 扩展路径：先单一高频闭环，验证后再复制
- 阶段 0：自然语言现货交易 + 一键钱包（解决"不敢用"）
- 阶段 1：自动策略（定投 / 止损 / 网格）+ 信号订阅（解决"不想盯盘"）
- 阶段 2：跨链自动收益 + x402 自动支付 + A2A 协作（解决"资产分散"）
- 阶段 3：非交易任务（研究 / 风控 / 信息流）开放给所有 Agent——从"交易"扩成"Agent 商业平台"

### 2.7 风险提示 & 护城河的具体形态
- 纯订阅卖信号容易变"韭菜收割"；且卖策略有监管风险（投资建议牌照）。
- 真正护城河 = **执行 + 反馈闭环**：黑盒信号不能直接给权限，只能给"建议"；执行与风控留在平台，形成可审计的链上历史与声誉。
- conclusion：**"你可以卖神秘武艺，但武器只能由平台来挥。"**——同时回答信任、监管、卖家 IP 三个问题。

**护城河落到地面上 = 一个「业绩信誉分」。这是我要新建的东西，不是重建 OKX.AI 已有的信誉。**

OKX.AI 的核心卖点之一就是 **"shared identity & unified reputation"**——每个 Agent 有单一链上身份，信誉跨 A2A / A2MCP 累积。**这是先发资产，我们要接住，而不是另起炉灶。** 但它记的是**履约信誉**（活交付了没有、有没有被拒收），而金融场景需要的是**业绩信誉**（赚没赚钱、回撤多大、样本够不够）。

后者有三个履约信誉完全不会遇到的难题：

| 难题 | 具体表现 |
|---|---|
| **① 抗操纵** | 刷单、多账号对倒、只上报盈利单——履约信誉没有这种作弊动机，业绩信誉有 |
| **② 口径统一** | 回测 vs 实盘、毛收益 vs 扣费后净收益、时间加权 vs 金额加权，必须全平台一套口径 |
| **③ 可验证** | 战绩必须由平台从执行账本自动生成，**不能由卖家自报** |

> **所以我的第一版就做一件事：把"业绩信誉"变成平台可自动计算、卖家不可篡改的一个分。**
> 数据源头已经在 §2.8.2 解决了——**Signal Vault 子钱包账本**。这个分不是孤立功能，它同时是三个机制的开关：**profit-share 的分成档位**（§2.8.1）、**订阅凭证的准入资格**（§2.8.3）、**marketplace 的搜索排序权重**。

### 2.8 分佣 · 归因 · 冷启动 · 责任设计

#### 2.8.1 分佣机制：profit-share 不是新方案，新的是"信号与执行解耦"

先说结论：**profit-share 在 OKX 已经跑了三年**——现货 / 合约跟单分润 8%–13%、策略带单最高 30%，按自然周结算、系统先预扣后退，这本身就是 high-water mark 的工程实现。所以我不论证"该不该用 profit-share"，我论证的是**它搬到 Agent 场景后什么变了**：

> profit-share 在跟单里能跑通，是因为**平台同时控制信号和执行**——带单员在 OKX 账户下单，跟单员在 OKX 账户跟随，P&L 由平台账本直接算，**归因成本为零**。
> 搬到 Agent 场景，**信号与执行第一次解耦了**：卖家只吐信号（黑盒 MCP，A2MCP 按次交付），买家在 CEX 还是链上执行、执不执行、按什么仓位执行，平台都不控制。
> **所以真正的难题不是"该不该分成"，是"分给谁、按什么口径算"。** 归因方案见 §2.8.2——这是整篇方案的地基，也是我认为 Agent 商业化落地的第一个工程难题。

沿用 profit-share + high-water mark（防同一笔利润重复抽佣），分档对标跟单：首期 10%–20%，随业绩信誉分上调，上限 30%。
- **不用 GMV 分成**：诱发刷量，且与"为结果付费"的激励方向相反。
- **订阅费不废掉**，但它解决的是另一个问题——见 §2.8.3。

#### 2.8.2 归因设计：信号 — 子钱包 — 账本（本方案的核心机制）

这是 Agent 场景做 profit-share 绕不开的第一个工程问题：**买家因这个信号赚了多少钱，平台怎么算？** 信号是黑盒、按次交付、买家执行场所不可控——从混合账户里做事后归因拆分，成本高到跑不动。

**解法是把 Agentic Wallet 已有的「最多 50 个子钱包、分仓隔离」，从"多策略并行工具"重新定义为「归因单元」：**

> 买家每订阅一个信号源，平台自动开一个专属子钱包（**Signal Vault**），规则只有一条：**该子钱包只接受这一个信号源的指令，只走这一个执行通道，资金不与其他信号源混用。**

一次性解决四件事：
1. **P&L 天然隔离**——不需要从混合账户里做归因拆分，**子钱包净值变化 = 该信号的盈亏**；
2. **high-water mark 的计算复杂度从 O(交易笔数) 降到 O(信号源数)**——每个 (买家, 卖家) 对只需维护一个标量，周度结算才跑得动；
3. **撤权即止损**——买家不认可该信号时，撤销该子钱包的会话授权即可，不影响其他信号源，也不用提现；
4. **审计边界清晰到子钱包粒度**——出事时"是信号的错还是执行的错"天然分层（见 §2.8.5）。

**代价要说清楚（这是取舍，不是免费午餐）**：
- 子钱包数上限 50 → 单个买家最多同时订阅 50 个信号源，**首期限 20 个**，按数据放宽；
- 资金被切成 N 份 → 单笔可用仓位变小，**小额资金的经济性变差**，需设子钱包最低注资金额（建议 $100 起）；
- 跨子钱包调拨会污染归因 → 默认禁止自动调拨，只允许用户手动且留痕。

**平台侧因此拿到的资产**：业绩信誉分不再由卖家自报，而是**从子钱包账本自动生成**（见 §2.8.4）——这是后面所有信任机制的数据源头。

#### 2.8.3 订阅凭证层：月度订阅是 OKX.AI 目前缺的一层，且是纯增量

一面沟通里提到的口径是"买家按月订阅付费给卖家、OKX 从中抽手续费"。这里有个必须先讲清楚的事实：**OKX.AI 目前没有订阅这个能力。** 官网现在只有两条结算轨：
- **A2MCP**——按次即时结算（MCP 调用一次收一次）；
- **A2A**——托管担保，验收后放款（用户 3 天验收期，超时默认通过）。

**两条轨都没有"时间"这个维度。** 要做月度订阅，需要新增一层 **订阅凭证（Subscription Entitlement）**，四个动作：

| 动作 | 机制 | 为什么必须有 |
|---|---|---|
| **发行** | 买家付款 → 平台签发链上凭证（买家 / 服务 / 生效期 / 调用配额） | 把"我付过钱"变成可验证状态 |
| **核销** | Agent 每次调该 MCP 前先验凭证，无凭证直接拒绝 | 订阅的价值边界在此划定 |
| **续费** | 到期前自动续签；到期即失效 | 现金流确定性的来源 |
| **中断即失效** | 买家撤权 / 退款 / 卖家被下架 → 凭证立即失效，MCP 侧实时拒绝 | **买家唯一的退出保障** |

**这层是纯增量**：不动现有两条轨，可独立上线。而且它正是 OKX 的抽成抓手——**凭证由平台签发，就绕不开平台**。

**订阅 vs profit-share：不是二选一，是两层叠加**
- **订阅费 = 现金流底盘**：可预测、好卖、平台好抽成，解决卖家"活下去"；
- **profit-share = 激励校准层**：解决"卖家只想让人续订、不关心结果"的劣币问题。

只用订阅 → 卖家堆"好看但不赚钱"的信号；只用 profit-share → 冷启动期卖家零收入，活不到有利润那天。
**首期两层都上，用「profit-share 占比随业绩信誉分递增」做动态平衡**：新卖家 100% 订阅制；信誉分达标后逐步切到"订阅 30% + 分成 70%"——既给卖家活下来的确定性现金流，又把平台和卖家的利益钉在"买家真赚钱"上。

#### 2.8.4 冷启动：双侧都冷，但买方侧握着别人抄不走的弹药

**先纠正一个前提**：这不是"买方不缺"的单侧冷启动。OKX.AI 当前的准入设计是 **"No OKX account is required to get started"**——买方是任意装了 Agent 客户端（OpenClaw / Hermes / Claude Code / Codex）的人，**账号体系与 CEX 尚未打通，买方完全在 OKX 漏斗之外**。

这是问题，也是**全篇最大的增长杠杆**：**把 OKX 1.5 亿存量 CEX 用户导进 OKX.AI，是别人抄不走的、现成的冷启动弹药。** 三招按优先级：

1. **一键开卡**：CEX 账户内一键创建 Agentic Wallet + 预置授信额度（有限合伙授权），跳过"建钱包 → 充值 → 授权"这条流失率最高的路径；
2. **模拟盘先行**：Agent Trade Kit 已有完整 demo 模式（80+ 工具全部可用）。CEX 首页挂"用模拟盘试跑一个 Agent 策略"，零资金门槛，跑出正收益再引导实盘——把"敢不敢"降级成"先看看"；
3. **跟单用户迁移**：现有跟单用户是**已经被教育过"为结果付费"**的人群，是 Agent 信号订阅的首批天然买家。用"你的带单员现在可以把它变成 7×24 的 Agent 服务"做定向触达——**这是从已有 profit-share 心智里长出来的增量，不是重新教育市场。**

**卖方侧（三件并行）**：
1. 首发 3–5 个有可追溯战绩的机构 / 大 V 卖家——承担"背书"而非"被补贴的运力"；
2. **官方不出信号，出标准**——见下方；
3. 卖家上架必须提供**平台可校验的**可追溯战绩（≥6 个月，回测与实盘分列），数据对不上直接驳回。

> **官方的两件事（都不带投资建议属性）**
> 1. **执行框架样板**：一套开源的"信号 → 执行 → 审计"参考实现，让买家看懂"信号怎么绑定执行、结果怎么审计"——这是**工具**，不是建议；
> 2. **审计与披露标准**：官方定义"可追溯战绩"的数据格式与校验口径（必披露指标、回测 vs 实盘的区分方式、样本量下限），**所有卖家按同一口径披露，且战绩由平台从子钱包账本自动生成**。
>
> 平台做**标准制定者和校验者，不做参与者**——一旦官方自己下场出信号，就成了平台提供投资建议，直接踩 §2.7 刚立的那条红线。

**为什么不是补贴**：这市场缺的是信任不是数量。补贴打在数量上会打水漂，打在背书和可验证战绩上才起飞轮。

#### 2.8.5 责任归属：三层划分 + 强制护栏 + 证据链

- **策略层（信号对错）→ 卖家责任**：强制披露历史胜率 / 回撤 / 样本数（由平台从子钱包账本自动生成，卖家不可篡改）；只赔虚假披露，不赔"没赚钱"。
- **执行层（下单 / 滑点 / 故障）→ 平台责任**：在 TEE 内跑、先模拟后执行、出不可篡改审计日志（谁签了哪条指令、Agent 是否原样执行）——出事时凭证据链把责任钉在"执行"这一层。
- **用户意志层（授权范围内亏损）→ 用户自负，但护栏默认开**：单笔 / 日累计限额默认开启且不可关闭、陌生地址转账强制双确认 + 风险分级拦截、授权随时可撤。

**出事后的处理，全部建立在审计日志上，不做无依据的安抚赔付**——"用钱买用户、同时回避责任认定"短期有效，长期会同时摧毁赔付纪律和用户信任：

| 情形 | 处理 | 依据 |
|---|---|---|
| 日志证明是**执行故障**（Agent 未原样执行、滑点超出承诺区间、平台侧服务中断） | 按 SLA **确定性赔付**，走平台责任，公开赔付口径 | 不可篡改审计日志 |
| 日志证明**执行忠实、亏损来自市场** | 不赔付，但**强制披露**：向用户出具该笔交易的完整执行回放 + 该信号的历史回撤分位 | 把"我亏了"变成"我看懂了为什么亏" |
| 日志**无法定责**的争议 | 走 OKX.AI 已有的**质押仲裁**（≥5 名仲裁者、质押 ≥100 OKB、少数服从多数），不搞平台暗箱 | 平台已有机制，复用而非新建 |

**前置动作**：新用户默认先进 demo 模拟盘（Agent Trade Kit 已有能力）跑通一轮再开实盘权限——把赔付事件在源头减少，比设计赔付规则更重要。

**合规：不能靠换收费形态解决**

需要特别纠正一个常见误解：**按业绩收费（performance-based fee）在多数法域比固定订阅更贴近"资产管理 / 代客理财"这类受监管活动，是加重情节，不是免责事由。** 把 profit-share 命名为"资产管理佣金"等于主动认领监管身份，方向是反的。真正的合规动作是三件事：

1. **服务定性**：卖的是"数据 / 信号 / 执行自动化工具"，不是"投资建议"。卖家端强制免责声明 + 禁止个性化推荐（不做"你应该买 X"，只做"信号：X 触发 Y 条件"）；
2. **地域白名单**：首期直接对齐 OKX 现有跟单的不可用地区清单（美国、英国、新加坡、中国香港、马来西亚等 14+ 地区），Agent 信号服务只在监管相对宽容的法域开放；
3. **把"业绩展示"和"业绩承诺"切开**：展示可追溯的历史战绩是**信息披露**（必须做，且平台校验）；承诺或暗示未来收益是**红线**（规则 + LLM 抽检 + 惩罚性下架）。用技术手段把这两者分开，而不是靠事后审核。

#### 2.8.6 OKX 的差异化：vs 自家跟单，不是 vs 别人

跟单是**内部最成功的先行产品，也是最近的竞品**——所以我不做替代，做扩展。三条规定边界：

| 维度 | 跟单（已有） | Agentic Wallet（要做的） |
|---|---|---|
| **信号源** | 人（带单员），必须实名 | 任意 Agent / MCP 服务，可以是黑盒 |
| **执行场所** | OKX 单一 CEX 账户内 | 链上 + CEX 跨场所，靠子钱包做归因隔离（§2.8.2） |
| **可编程性** | 固定跟随比例 | 限额 / 期限 / 白名单 / 撤权全部可编程，买家保留最终否决权 |

> 一句话：**跟单证明了"为结果付费"成立，我们要做的是让它可编程、可组合、可跨场所。**

---

## 第三部分：Agentic Wallet 运营方案

### 3.1 接链选择与 BD 协作

**决策逻辑**：**"在哪里最容易产生第一个真实、反复使用的 Agent 交易用户，就先接哪里。"** 热度不是标准，种子用户密度才是（冷启动瓶颈不是"多一条链可夸"，而是"能否跑通一个真实闭环"）。

**四个候选打分（X Layer / Solana / Base / 某条新 L1），带权重与总分（1–5 分）**：

| 标准（权重） | X Layer | Solana | Base | 新 L1 |
|---|---|---|---|---|
| ① 种子用户密度（30%） | 1 —— 尚无 Agent 生态 | **5** —— ElizaOS(ai16z) 主场，pump.fun 养出的"Agent 发币 + 自主交易"文化，已存在大量 Agent 持有资产、自动交易的真实用户——用户已被教育完，只差执行层 | 3 —— 有 Virtuals 等 Agent 生态，但偏内容/娱乐向，交易密度低 | 1 —— 基本为零，连"有没有人用 Agent 交易"都要赌 |
| ② 开发者基建（20%） | 4 —— EVM 兼容、自家可控 | **5** —— SDK 成熟、文档全、Jupiter 聚合器打通 DEX 流动性、发币/授权/转账工具链齐 | 4 —— EVM 工具链成熟，但无相对优势 | 2 —— 基建往往是最大坑：接得动但生态稀薄，接完无人二次集成 |
| ③ 商业闭环（20%） | **5** —— 零 gas、单笔可低至 1 美分，微支付在经济账上完全成立；结算自有 | 4 —— 低费高吞吐，微支付 / 高频小额成立 | 3 —— gas 不高但依赖跨链 | 2 —— gas 可能优惠，但没流量，闭环推不动 |
| ④ **竞争位势（30%）** | **5** —— 自家链，无对手、可控、可改 | **4** —— Coinbase 的 Base 在 EVM 侧已占 >90% agentic 稳定币量，**Solana 侧尚是空白** | 1 —— **x402 + Base 已是 Coinbase 主场，我们进去是客场作战** | 3 —— 无对手也无用户 |
| **加权总分** | **3.6** | **4.5** | 2.6 | 2.0 |

> **结论：先接 Solana——不是最大牌的链，而是"第一个真实闭环最可能诞生的链"，且是 Coinbase 尚未占据的一侧（见 §3.3）。**
> **但落地顺序是 X Layer 先、Solana 后。** X Layer 总分第二，但它有一票否决级的优势：**零 gas + 结算自有 + 完全可控**，适合当"内部打样"——先在自家链跑通 Agent 支付 / 交易闭环，拿到真实数据，再拿样板去 Solana 复制。**用内部证据对外谈判，比用 PPT 谈有说服力得多。**
> **Base 明确后置**：那是 Coinbase 的主场，在客场打补贴战不划算。但 Base 必须作为 **x402 兼容性验证**的测试对象——协议层跟随（§3.3.2）不是口号，要跑得通才算数。

**BD 协作切分**：核心 = **按漏斗分段，不按赢家通吃**。
- BD 背**上游**：签约链/协议数量、导入的项目方和开发者资源（"把门打开，把人请进客厅"）。
- 你背**下游**：导入资源产生的激活数 / 活跃交易用户 / 手续费（"人进来之后坐下来交易"）。
- **共享一张转化报表**：签约 → 上线 → 激活 → 成交四段一起看，以"签约后 30/60/90 天达到 X 个活跃交易用户"为共同红线口径。
- conclusion："BD 为入口负责，我为出口负责；谁都不抢对方的指标，但谁都躲不开同一张转化表——这条链签了却没交易，我俩一起向老板交代。"（同时回答"怎么切"和"怎么防甩锅"）

### 3.2 数据驱动的迭代闭环

> "第一周我不看'总数为什么不涨'，先拆三段漏斗：**周新增用户数、新增→首笔成交转化率、次周留存**。三个数分别对应获客、激活、留存，一眼定位问题在哪一段。
>
> 假设我看到的数字是：周新增没跌、首笔成交转化率没跌，但 **次周留存从 40% 掉到 22%**。那问题就不是获客（人还在进来），也不是下单难（进来的人一样能成交），而是**留存**——用过一次的用户下星期不回来了。
>
> 我再切片定位到'为什么不留存'，按分群看：**跟单信号的用户留存高（60%），自己动手下单的用户留存低（10%）**。方向就出来了——问题不在交易本身，在自主下单路径。
>
> 再往下挖一层：这波低留存用户`完成首笔交易 → 第二次交易`的流失点，发现**第一次授权/确认弹窗环节流失最多**——小白被一串签名和授权选项吓退，成交一次就再也不敢碰。
>
> 于是研发优先级很明确：**重做首笔授权体验**——用'模拟演示 + 一键授权 + 限额默认开启'收走弹窗复杂度，本版本验收指标定为'新用户 4 周留存 ≥ X%'（而不是再冲新客）。上线后我盯留存曲线和流失点转化率，不回弹说明方向对了。"

### 3.3 竞争防御策略

#### 3.3.1 先承认一个不舒服的事实：协议层不在我们手里

讨论护城河之前，必须先摆一组数据（Coinbase 2026 Q2）：

- **>97%** 的链上 agentic 交易跑在 **x402** 上，而 **x402 是 Coinbase 的协议**；
- **>90%** 的 agentic 稳定币交易量跑在 **Base**；
- **>99%** 的 agentic commerce 用 **USDC** 结算。

**而 OKX 的 Agentic Wallet 自动支付，本身就是跑在 x402 上的。**

所以"我们的护城河是信任"这句话，如果不先回答"协议层在别人手里怎么办"，就是一句空话——**最坏情况下，我们是在 Coinbase 定义的赛道上做应用层。** 这个前提不能回避，回避了后面所有论证都不成立。

#### 3.3.2 我的判断：协议层跟随，在它之上的三层卡位

**协议层不做对抗。** 我不会提议做 x402 的替代品——那只会分裂生态、把开发者推向对面，而且我们赢不了一个已经占住 97% 份额的协议。**跟随，并把兼容性成本转嫁成优势：x402 用得越广，支持 x402 的 Agentic Wallet 接入面就越大。**

真正可卡位的是协议层之上的三层，而这三层 Coinbase 一个都没占：

| 层 | 为什么 Coinbase 没占 | OKX 凭什么能占 |
|---|---|---|
| **① 执行质量层** | x402 解决"能不能付"，不解决"付得划不划算"——它不做路由优化、不做滑点控制、不做失败重试 | **这是 OKX 的老本行**：Onchain OS 聚合 **500+ DEX**、日交易量 **$3 亿**、**<100ms**、99.9% 可用性。同样的 x402 调用，谁能拿到更好的路由价格、更低的滑点、更少的失败交易——**这一层是硬指标，可量化、可对比、可做对比页** |
| **② 业绩信誉层** | Coinbase 的 agentic 量主要是**支付与结算**，它没有"这个 Agent 的策略赚不赚钱"这个信号 | 金融场景真正稀缺的是业绩信誉，不是支付通道。而**数据源头我们已经解决了**——Signal Vault 子钱包账本自动生成的**业绩信誉分**（§2.7 / §2.8.2）。这是它要从零起步的东西 |
| **③ 供需匹配层** | Coinbase 有支付轨，没有"信号卖家 ⇄ 买家"的双边市场，也没有分佣机制 | **OKX 跟单跑了三年，profit-share 的用户心智和结算工程都是现成的**（分润 8%–13% / 策略带单最高 30%、周度结算、先扣后退）。我们要做的是**外溢，不是从零教育市场** |

> **一句话：协议层跟随，执行质量 / 业绩信誉 / 供需匹配三层卡位。x402 越普及，我们的执行层越值钱——因为支付的归支付，执行的归执行。**

#### 3.3.3 竞品地图：对手到底是谁

| 竞品 | 打法 | 威胁 | 我的应对 |
|---|---|---|---|
| **Coinbase / Base + x402** | 协议 + 稳定币 + 合规入口 | **最高** | 协议层跟随，三层卡位（§3.3.2）。它的软肋是**只有支付，没有交易深度和业绩信号** |
| **Telegram 交易机器人**（Banana Gun / Maestro / Trojan） | 才是今天"小白自动交易"的**真实 incumbent**，已跑通订阅 + 手续费分成 | **高，且最容易被忽略** | 它们解决"下单快"，不解决"敢把钱交给它"——**无 TEE、无限额、无撤销、无审计**。我们的差异化是**可编程授权 + 一键撤权 + 可审计战绩**，这是它们的产品形态从根上做不到的 |
| **CEX 跟单**（Bybit / Binance / **OKX 自己**） | 已有 profit-share 心智 | 中（**内部竞品**） | 不做替代做扩展，边界见 §2.8.6 |
| **Solana Agent 生态**（ai16z / ElizaOS、Virtuals、pump.fun） | Agent 发币 + 自主交易文化 | 中（**亦敌亦友**） | 是我们要接的生态（§3.1），也随时可能自建执行层——**所以要抢先成为它们的默认执行层，而不是等它们自己做** |

#### 3.3.4 结论：不打价格战，打"第一次试完之后"

竞品未来可能全面免费、接链更多——但**免费和多链解决的都是表层竞争**：

> 免费解决的是**价格**，不解决**信任**——用户敢不敢把钱交给 Agent，跟对手收不收费无关。
> 多链解决的是**覆盖**，不解决**执行质量**——链再多，执行不忠实、不透明，数据还是留不住。
>
> 所以我不跟它在"谁能更便宜、谁覆盖更广"上打，在 **"用户第一次试完谁能留下来"** 上打：**可审计的业绩信誉分 + 模拟盘先验 + 可编程授权与一键撤权 + 更好的路由成交价**——这些是它免费送不起、多链换不来的。
>
> **价格战我认输，护城河我认赢。**