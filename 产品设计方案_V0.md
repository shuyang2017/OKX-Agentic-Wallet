# OKX Agentic Wallet PM · 产品设计方案

> **TL;DR（30 秒读完）**
> - **核心判断**：Agent 钱包的唯一瓶颈不是功能，是**信任**。先解决"敢不敢把钱交给 Agent"，再谈量——初版只做：可信执行通道 + 可编程授权 + 标准接口。
> - **商业逻辑**：平台垄断 Agent 经济中供需交互的三个必经收费站（交易手续费 / 利润分成 / 结算通道费），把钱包做成 Agent 的执行层与价值层。
> - **我的第一件事**：先在一条链上跑通一个真实高频闭环（内部 X Layer 打样 → Solana 复制），拿到数据和用户后再扩展——不铺摊子。
> - **一句话定位**：跟单复制"人"，OKX 做可编程、可审计、可追踪的链上执行层——信号可以黑盒，每一次执行都透明。

## 第一部分：OKX 现状 & What should OKX do first

### 1.1 OKX 现状
1. OKX 2026 两件新品：**Agentic Wallet**（链上，为 Agent 设计，TEE + 模拟 + 风险分级）+ **Agent Trade Kit**（CEX，MCP 自然语言下单）。
2. OKX 正在把钱包的能力开放给 AI Agent，让 Agent 自己持有、自主交易，同时用授权限额、先模拟、风险分级、TEE 私钥隔离给足护栏。

### 1.2 从第一性原理推出"Agentic Wallet 做什么"
1. 最底层约束 = **信任**：把真金白银的私钥 / 授权交给一个不可控的 Agent，没人敢。
2. 所以问题不是"接多少条链"，而是"Agent 需要什么、风险怎么兜住"。
3. 初版只做三件事：**一个可信执行通道 + 一套可编程授权（限额 / 期限 / 白名单）+ 一组 Agent 可调用的标准接口**，外加先模拟、风险分级、TEE 兜底。
4. 先跑通一条链上的真实用例（如 Agent 自动支付 / 自动申购），拿到数据再扩链。

→ conclusion:
> 把「信任」这个瓶颈优先解决，而不是先扩功能。
> 站在 AI × Web3 交汇点，把钱包做成 Agent 的执行和价值层。

---

## 第二部分：Agentic Wallet 产品设计

### 2.1 背景：为什么 OKX 现在必须做 Agent

**"OKX 收不到费"的现状：**
- 链上交易：gas 全给矿工 / 验证者、流动性费给 DEX / LP，**OKX Web3 DEX 聚合器目前限时免服务费**；DEX API 对集成方按 20% 分成 / 正滑点收费，但散户端几乎不可见。
- CEX：现货 maker / taker 仅 0.08% / 0.10%、合约 0.02% / 0.05%，散户手续费薄，大头在机构做市商。
- **结论：现有收入 = "用户主动高频交易才收得到钱"。** 用户越自动化越躺平，这池子越萎缩——这正是 OKX 押注 Agent 方向的动因：**换一条收钱的路：代理结算 + 订阅服务费。**

**官方已在落地的产品：**
- **Agentic Wallet**：TEE 自托管、会话密钥自动签名、20+ 链——"钱包给 Agent 用"已上线。
- **Agent Trade Kit**：Agent 经 MCP 直接在 CEX 下现货 / 合约 / 期权单，带 Demo 模拟盘。
- **Onchain OS + Agent Payments Protocol（APP）**：Agent 支付标准，X Layer 零 gas、单笔可低至 1 美分，支持 escrow / 按调用 / HTTP 402。
- **OKX.AI marketplace**（2026-06 上线）：Agent Marketplace + Task Marketplace 双场；A2MCP（按次即时结算）与 A2A（托管担保 + 仲裁）两种模式，评估员质押 OKB 仲裁；**当前服务费为 0，但协议公开预留收费权**。
- **参照系已有先例**：Binance / Bybit 跟单交易 = 信号卖家拿 10–30% 利润分成 + 手续费佣金。"卖信号"模式成熟，OKX 做的是它的 AI 升级版。

早期生态合作方 CoinAnk 即"链上实时行情数据按次收费"——**"卖家经 MCP 卖信号、黑盒不透传、买家付费订阅"就是 OKX.AI 的 A2MCP 形态。**

### 2.2 产品定位：Codex 的壳 + Siri 的门槛
- **"Codex 的壳 + Siri 的门槛"**——人话进去、链上结果出来、中间全帮用户做完。

### 2.3 产品骨架
- **要卖的结果** = 用户达成目标（买指定币 / 止损 / 定投 / 付账 / 资产增值），不是钱包，也不是代码。
- **核心瓶颈** = 认知门槛（看不懂代码 / 合约 / 授权）**×** 信任（链上不可逆，一笔错全没）。
- **解法 = "意图"与"链上执行"之间加三层**：

```
用户(人话) → ①意图层   → ②护栏层   → ③执行层 → 结果 + 可追踪记录
            解意思/拆步骤  模拟·限额·分级·TEE   Agent Trade Kit(CEX)
            面向小白不卡壳   信任问题唯一解       Onchain OS(链上)/APP(支付)
```

### 2.4 用户分层：怎么让不同水平用户都能用
| 用户层 | 真实诉求 | 产品形态 | 对应官方部件 |
|---|---|---|---|
| 完全小白 | "我想买点比特币" | 对话交易 + 一键钱包 | Agentic Wallet |
| 普通用户 | "跌破 9 万就买，8.5 万止损" | 意图命令（条件单） | Agent Trade Kit / DEX 聚合 |
| 进阶用户 | "帮我按这信号定投" | 订阅 marketplace 信号 | A2MCP 黑盒服务 |
| 卖家 / 开发者 | "我的策略想卖订阅" | 黑盒上架，计费托管平台负责，逻辑不透传 | Onchain OS + APP |

### 2.5 商业闭环：三腿收钱（OKX 为什么赚得到钱）
1. 交易手续费——Agent 替用户高频买，量上去了。
2. 卖家收入抽成（利润分成 profit-share）——按卖家真实利润分成，订阅仅作为"解锁信号库"的入场费。
3. Agent 结算通道费——escrow / 微支付通道。

正是把 Coinbase"订阅 + 服务收入占 35%"的模式复制到链上。

**平台垄断了 Agent 经济里供需交互的三个必经环节，每个环节是独立收费站：**
- 执行引擎（撮合 / 路由 / 结算）堵住**资金流** → 按交易额抽成 = ① 交易手续费
- Marketplace（双边网络）堵住**信息 / 供需流** → 按卖家收入分成 take rate = ② 利润分成（profit-share，收的是**卖家**的钱，订阅仅作入场费）
- 托管 / 担保 / 仲裁（escrow）堵住**风险流** → 按担保 / 结算金额收费 = ③ 结算通道费
- 商业学本质：三种价值单元互相独立——①按"动作价值"计费、②按"卖家持续收入"计费、③按"风险承担量"计费，谁都不是谁的别名。
- 类比收口：交易手续费 = 银行通道费；利润分成 = App Store 的 30%；结算通道费 = escrow 担保费。

### 2.6 扩展路径：先单一高频闭环，验证后再复制
- 阶段 0：自然语言现货交易 + 一键钱包（解决"不敢用"）
- 阶段 1：自动策略（定投 / 止损 / 网格）+ 信号订阅（解决"不想盯盘"）
- 阶段 2：跨链自动收益 + x402 自动支付 + A2A 协作（解决"资产分散"）
- 阶段 3：非交易任务（研究 / 风控 / 信息流）开放给所有 Agent——从"交易"扩成"Agent 商业平台"

### 2.7 风险提示
- 纯订阅卖信号容易变"韭菜收割"；且卖策略有监管风险（投资建议牌照）。
- 真正护城河 = **执行 + 反馈闭环**：黑盒信号不能直接给权限，只能给"建议"；执行与风控留在平台，形成可审计的链上历史与声誉。
- conclusion：**"你可以卖神秘武艺，但武器只能由平台来挥。"**——同时回答信任、监管、卖家 IP 三个问题。

### 2.8 分佣 · 冷启动 · 责任设计

**分佣机制**：卖家拿**利润分成 profit-share**（对标 Bybit 10–30% + high-water mark 高水位线，防同一笔利润重复抽佣），订阅仅作"解锁信号库"的入场费，不作主要分成来源。
- 为什么不用纯订阅：会制造激励错配——卖家只想让人续订，不关心交易结果 → 堆满"好看但不赚钱"的信号，劣币驱逐良币。
- 为什么不用 GMV 分成：诱发刷量。
- 为什么 profit-share 对：三方利益同向——买家赚钱才付费、卖家在买家赚钱时才有分成、平台靠成交收手续费。合规口径也更干净（可界定为"资产管理佣金"，避开"投资建议牌照"红线）。

**冷启动（死循环的破解）**：缺口不在买方（CEX 存量用户垫底），在**卖方信任**。三位并行：
1. 首发 3–5 个有可追溯战绩的机构 / 大 V 卖家——承担"背书"而非"被补贴的运力"。
2. 官方自造血 1–2 个样板信号（在平台监督下运行）——让买家先看懂"信号怎么绑定执行、结果怎么审计"。
3. 卖家上架必须提供 ≥ 若干月的可追溯回测 + 实盘截图，平台校验数据，伪的驳回。
- 为什么不是补贴：这市场缺的是信任不是数量，补贴打在数量上打水漂，打在背书上才起飞轮。

**责任归属（三层划分 + 强制护栏 + 证据链）**：
- 策略层（信号对错）→ **卖家责任**：强制披露历史胜率 / 回撤 / 样本数；只赔虚假披露，不赔"没赚钱"。
- 执行层（下单 / 滑点 / 故障）→ **平台责任**：在 TEE 内跑、先模拟后执行、出不可篡改审计日志（谁签了哪条指令、Agent 是否原样执行）——出事时凭证据链把责任钉在"执行"这一层。
- 用户意志层（授权范围内亏损）→ **用户自负，但护栏默认开**：单笔 / 日累计限额默认开启且不可关闭、陌生地址转账强制双确认 + 风险分级拦截、授权随时可撤。
- 出事后的三道工序：法务口径"策略风险 vs 执行故障"分层 → 小额 compassionate 补偿（不承认责任但保住"小白敢用"）→ PR 引导去 Demo 模拟盘先跑（Agent Trade Kit 已有能力）。

**OKX可以做的“差异化”**：跟单复制"人"；OKX做的是**可编程、可审计、可追踪的链上执行层**——信号可以黑盒，但每一次执行都透明。

---

## 第三部分：Agentic Wallet 运营方案

### 3.1 接链选择与 BD 协作

**决策逻辑**：**"在哪里最容易产生第一个真实、反复使用的 Agent 交易用户，就先接哪里。"** 热度不是标准，种子用户密度才是（冷启动瓶颈不是"多一条链可夸"，而是"能否跑通一个真实闭环"）。

**三个候选打分（Solana / 某条新 L1 / 熟的 EVM 链如 Base）**：
| 标准 | Solana | Base | 新 L1 |
|---|---|---|---|
| ① 种子用户密度 | 最高：ElizaOS(ai16z) 主场，pump.fun 养出的"Agent 发币+自主交易"文化，已存在大量 Agent 持有资产、自动交易的真实用户——用户已被教育完，只差执行层 | 有 Virtuals 等 Agent 生态，但偏内容/娱乐向，交易密度低 | 基本为零，连"有没有人用 Agent 交易"都要赌 → 直接排除 |
| ② 开发者基建 | SDK 成熟、文档全、Jupiter 聚合器打通 DEX 流动性、发币/授权/转账工具链齐——接入成本最低 | EVM 工具链也成熟，但无相对优势 | 基建往往是最大坑：接得动但生态稀薄，接完无人二次集成 |
| ③ 商业闭环 | 低费高吞吐，微支付/高频小额在经济账上成立，与"Agent 代替人高频交易"的假设天然匹配 | gas 不高但依赖跨链 | gas 可能优惠，但没流量，闭环推不动 |

> **结论：先接 Solana——不是最大牌的链，而是"第一个真实闭环最可能诞生的链"。** 加分补充：自家 X Layer（零 gas、APP 落地链）当"内部打样"，先在自家链跑通 Agent 支付/交易闭环，再拿样板去 Solana 复制——用内部证据对外谈判。

**BD 协作切分**：核心 = **按漏斗分段，不按赢家通吃**。
- BD 背**上游**：签约链/协议数量、导入的项目方和开发者资源（"把门打开，把人请进客厅"）。
- 你背**下游**：导入资源产生的激活数 / 活跃交易用户 / 手续费（"人进来之后坐下来交易"）。
- **共享一张转化报表**：签约 → 上线 → 激活 → 成交四段一起看，以"签约后 30/60/90 天达到 X 个活跃交易用户"为共同红线口径。
- conclusion："BD 为入口负责，我为出口负责；谁都不抢对方的指标，但谁都躲不开同一张转化表——这条链签了却没交易，我俩一起向老板交代。"（同时回答"怎么切"和"怎么防甩锅"）

### 3.2 数据驱动的迭代闭环

> "第一周我不看'总数为什么不涨'，先拆三段漏斗：**周新增用户数、新增→首笔成交转化率、次周留存**。三个数分别对应获客、激活、留存，一眼定位问题在哪一段。
>
> 假设我看到的数字是：周新增没跌、首笔成交转化率没跌，但 **次周留存从 40% 掉到 22%**。那问题就不是获客（人还在进来），也不是下单难（进来的人一样能成交），而是**留存**——用过一次的用户下星期不回来了。
>
> 我再切片定位到'为什么不留存'，按分群看：**跟单信号的用户留存高（60%），自己动手下单的用户留存低（10%）**。方向就出来了——问题不在交易本身，在自主下单路径。
>
> 再往下挖一层：这波低留存用户`完成首笔交易 → 第二次交易`的流失点，发现**第一次授权/确认弹窗环节流失最多**——小白被一串签名和授权选项吓退，成交一次就再也不敢碰。
>
> 于是研发优先级很明确：**重做首笔授权体验**——用'模拟演示 + 一键授权 + 限额默认开启'收走弹窗复杂度，本版本验收指标定为'新用户 4 周留存 ≥ X%'（而不是再冲新客）。上线后我盯留存曲线和流失点转化率，不回弹说明方向对了。"

### 3.3 竞争防御策略

**核心判断**：竞品未来可能全面免费、接链更多——但 OKX 不打价格战。免费和多链解决的是表层竞争，真正留人的是**信任 + 执行质量**：**"用户第一次试完谁能留下来"，谁就赢。**

**第一性原理拆解**：
> "免费解决的是**价格**，不解决**信任**——用户敢不敢把钱交给 Agent，跟对手收不收费无关。多链解决的是**覆盖**，不解决**执行质量**——链再多，执行不忠实、不透明，数据还是留不住。
> 所以我不跟它在'谁能更便宜、谁覆盖更广'上打，在 **'用户第一次试完谁能留下来'** 上打：可审计战绩 + 模拟盘先验 + 信任结算通道——这些是它免费送不起、多链换不来的。
> 价格战我认输，护城河我认赢。"